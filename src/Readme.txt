Organization:

application -- main function
db -- database manager
gui -- all GUI classes
music -- music manager (list, data, searches)
network -- data exchanges
settings -- user & application settings manager
tools -- generic tools